import { constNum1 } from '@zsgg/recipe-package--utils';

const PlaygroundRecipeUtil = (): JSX.Element => (
    <div>
        <h1>PlaygroundRecipeUtil</h1>
        <div>constNum1: {constNum1}</div>
    </div>
);

export default PlaygroundRecipeUtil;
